## 03-interface

객체 = 변수 + 함수

object = property + method

```
let st1 = {
  kor : 100,
  eng : 90,
  avr : func(){},
}
```

위와 같은 코드에서도 타입을 지정해주자 -> interface

interface와 type의 결정적 차이

- interface는 클래스형태이다. 함수도 넣고, 그 함수를 그대로 확장 할 수 있기에 좀 더 복잡한 작업에는 interface 사용
- 다양한 형태로 내 마음대로 나만의 형태가 필요한건 type.
